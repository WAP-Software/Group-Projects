name = "finance-collab-files"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "finance-collab-files"

[vars]
ALLOWED_ORIGIN = "https://finance-collab.vercel.app"

# Secrets (set via: npx wrangler secret put <NAME>)
# SUPABASE_JWT_SECRET
